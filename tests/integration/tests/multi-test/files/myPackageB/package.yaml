spec-version: 0.36.0

name: cyclic

dependencies:
- base

library:
  exposed-modules:
  - Cyclic

tests:
  cyclic-test-suite:
    main: Spec.hs
    dependencies:
    - myPackageA
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
