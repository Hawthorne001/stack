spec-version: 0.36.0

# Only builds if this package name comes before myPackageA, alphabetically!
name: 0myPackageB

dependencies:
- base

library:
  source-dirs: src

tests:
  test:
    source-dirs: test
    main: Main.hs
    dependencies:
    - myPackageA
