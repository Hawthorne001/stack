spec-version: 0.36.0

name: myPackage

dependencies:
- base

library:
  source-dirs: src
  dependencies:
  - filemanip

executables:
  myExe:
    source-dirs: app
    main: Main.hs
    dependencies:
    - myPackage
